"use strict";

var React = require('react');

var Footer = React.createClass({
   render: function() {
       return (
         <footer className="footer">
           <footer className="container">
             <p className="text-muted">Made by Richard</p>
           </footer>
         </footer>
       );
     }
});

module.exports = Footer;
